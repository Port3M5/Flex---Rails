<h1>Images</h1>
<% @images.each do |image|%>
<h2><%= link_to image.title, [@category, image] %></h2>
<p><%= image.category_id %></p>
<p><%= image_tag image.url %></p>
<% end %>

<%= link_to "Add new", new_category_image_path %>